generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model StopArrival {
  id        Int      @id @default(autoincrement())
  busId     String
  routeId   String
  stopId    String
  timestamp DateTime @default(now())
}
